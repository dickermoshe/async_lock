name: qm
description: Locked Async Notifiers for Queries and Mutations
version: 0.1.0

environment:
  sdk: ^3.9.0
  flutter: ">=1.17.0"

dependencies:
  equatable: ^2.0.0
  flutter:
    sdk: flutter
  locked_async: ^0.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: "any"

dependency_overrides:
  locked_async:
    path: ../
